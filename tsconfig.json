{
    "compilerOptions": {
        "target": "ES2017",
        "lib": [
            "dom",
            "dom.iterable",
            "esnext"
        ],
        "allowJs": true,
        "skipLibCheck": true,
        "strict": true,
        "noEmit": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "react-jsx",
        "incremental": true,
        "plugins": [
            {
                "name": "next"
            }
        ],
        "paths": {
            "@/*": [
                "./src/*"
            ]
        }
    },
    "include": [
        "**/*.ts",
        "**/*.tsx",
        "next-env.d.ts",
        "next.config.mjs",
        ".next/types/**/*.ts",
        ".next/dev/types/**/*.ts"
    ],
    "exclude": [
        // The vendored Rust crate ships TypeScript examples with their own
        // dependencies. Left in the program they put unresolved imports in the
        // type check, which `npm run build` runs — the same way a copied site
        // dump under `indus.sarvam.ai/` broke it once already.
        "bridge/vendor",
        // The React Bits Shader Template, kept as a reference. It has its
        // own `@/*` paths that resolve to its own tree, and lucide-react
        // which this project does not install — so every one of its files
        // fails to resolve in our program. Third time this shape has
        // broken the build: `indus.sarvam.ai`, then `bridge/vendor`.
        "HOMEPAGE_REF",
        "node_modules",
        "supabase/functions",
        "packages",
        "examples",
        "indus.sarvam.ai"
    ]
}
