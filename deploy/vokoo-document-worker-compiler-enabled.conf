[Service]
# Opt-in acceptance override. The base unit stays compiler-disabled so a normal
# binary rollout cannot start model work before migrations and health checks.
Environment=VOKOO_COMPILER_ENABLED=true
