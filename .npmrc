# Font Awesome Kit packages live on their own registry. The token is read from
# the environment rather than written here, so this file is safe to commit.
@awesome.me:registry=https://npm.fontawesome.com/
//npm.fontawesome.com/:_authToken=${FA_PACKAGE_TOKEN}
