import type { ShaderVariantId } from "@/lib/marketing/shader-variants";

export const features = {
  smoothScroll: true,
} as const;

export const SHADER_VARIANT_DEFAULT: ShaderVariantId = "sarvathra";
