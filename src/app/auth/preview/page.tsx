// Scratch route so the sign-in screen can be looked at without signing out.
// Delete when the design is settled.
import { SignInScreen } from "@/components/application/auth/sign-in-screen";

export default function Page() {
    return <SignInScreen />;
}
