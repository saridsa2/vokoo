import { OperatorScreen } from "@/components/application/screens/operator-screen";

/** Every tenant on the platform. Gated in the database, not by this route. */
export default function OperatorPage() {
    return <OperatorScreen />;
}
