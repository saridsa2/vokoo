import { DashboardScreen } from "@/components/application/screens/dashboard-screen";

/** What the line is doing right now, and what the day has come to. */
export default function DashboardPage() {
    return <DashboardScreen />;
}
