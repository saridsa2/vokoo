/* eslint-disable @typescript-eslint/no-explicit-any */
// Stub — see agent-actions.ts.
export async function generateInfraAction(..._args: any[]): Promise<any> { return { ok: false, error: "Infra generation is not wired up in VoKoo." }; }
