import { PlatformPacksScreen } from "@/components/application/screens/platform-packs";

/** What a workspace is built from on the day it signs up. */
export default function PlatformPacksPage() {
    return <PlatformPacksScreen />;
}
