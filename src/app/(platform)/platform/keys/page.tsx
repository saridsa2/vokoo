import { PlatformKeysScreen } from "@/components/application/screens/platform-supply";

/** The provider accounts a tenant's calls run on when it brings none of its own. */
export default function PlatformKeysPage() {
    return <PlatformKeysScreen />;
}
