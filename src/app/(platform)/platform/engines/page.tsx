import { PlatformEnginesScreen } from "@/components/application/screens/platform-engines";

/** The engines the platform sells, and what a minute on each is sold for. */
export default function PlatformEnginesPage() {
    return <PlatformEnginesScreen />;
}
