import { NumbersScreen } from "@/components/application/screens/platform-supply";

/** The numbers this platform has bought, and who holds each one. */
export default function PlatformNumbersPage() {
    return <NumbersScreen />;
}
